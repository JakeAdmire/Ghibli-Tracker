export const userReducer = (state = {}, action) => {
  switch(action.type) {

    case('LOG_IN'):
      return 
      // action.user {email: '', password: ''}
      // if ()

    default:
      return state;
  }
}